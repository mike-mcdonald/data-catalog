{% include 'field.html.twig' with {
  label: 'Updated on'|t,
  layout_classes: label_display == 'inline' ? ['inline-flex', 'gap-1'] : ['flex', 'flex-col'],
  field_classes: field_classes|default([
    'text-sm',
  ]),
  title_classes: [
    label_display == 'visually_hidden' ? 'visually-hidden',
  ],
} %}
